export const BOARD_SIZE = 6; // Number of rows and columns in Triarii
export const DEFAULT_PIECES_REMAINING = 1000; // Arbitrary number larger than the max stack size

export const WHITE_PINNING = 1;
export const BLACK_PINNING = -1;
export const NO_PIN = 0;